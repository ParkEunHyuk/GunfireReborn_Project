파티클이 많이 꼬여 있어서 순서를 한번 적어보겠음

로더에

텍스쳐들 작성 (당연하겠지만 포인트 인스턴싱 버퍼가 존재하고 있어야함+ 버퍼를 담아둘 객체도)

포인트 인스턴싱이 들어있는 객체에 인스턴싱 버퍼에 보낼 DESC 작성 및 텍스쳐와 포지션 지정

당연하겠지만 포인트 인스턴싱 버퍼에도 파티클의 동작들을 지정해둬야함



로더 -> 유니버설_포인트_인스턴싱 -> 버퍼_포인트_인스턴싱 -> 객체 생성